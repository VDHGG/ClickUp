apiVersion: v1
kind: Service
metadata:
  name: clickup-web
  labels:
    app: clickup-web
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: clickup-web

